<%= form_for @user, url: update_password_path(@user.id) do |f|%>
  <%= f.label "Current Password"%><br>
  <%= f.password_field :old_password, class:'mb-3'%><br>
  <%= render "users/password", f: f %>
  <%= f.submit %>
<%end%>
