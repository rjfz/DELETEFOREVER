<%= form_for @role, url: admin_role_path(@role) do |f|%>
  <%= f.label :role%><br>
  <%= f.text_field :role, class:'mb-3'%><br>
  <%= f.submit class: 'mb-3' %><br>
  <%= link_to('Destroy', admin_role_path(@role),  method: :delete, data: { confirm: 'Are you sure?' }, class:  "btn btn-primary") %>
<% end %>
